const orders=JSON.parse(localStorage.getItem('orders'))||[];const box=document.getElementById('ordersList');if(!orders.length){box.innerHTML='<div class="empty"><h2>No orders yet 📦</h2><p>Place an order and it will appear here.</p><a href="index.html">Start Shopping</a></div>'}else{box.innerHTML=orders.flatMap(o=>o.items.map(item=>`<div class="order-card"><img src="${item.image}" alt="${item.name}"><div class="order-details"><h2>${item.name}</h2><div class="price">${item.price}</div><p class="status">✅ ${o.status}</p><p>Order ID: ${o.id}</p><p>Order Date: ${o.date}</p><p>Payment: ${o.payment}</p><button class="track" onclick="trackOrder('${o.id}')">Track Order</button></div></div>`)).join('')}function trackOrder(id){alert(`Order ${id}\nStatus: Your order is ${orders.find(o=>o.id===id)?.status||'placed'}.`)}window.trackOrder=trackOrder;
