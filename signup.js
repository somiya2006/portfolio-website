function signup(){const name=document.getElementById('name').value.trim(),email=document.getElementById('email').value.trim(),mobile=document.getElementById('mobile').value.trim(),password=document.getElementById('password').value,confirmPassword=document.getElementById('confirmPassword').value;if(!name||!email||!mobile||!password||!confirmPassword){alert('Please fill all fields.');return}if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){alert('Enter a valid email.');return}if(!/^\d{10}$/.test(mobile)){alert('Enter a valid 10-digit mobile number.');return}if(password.length<6){alert('Password must be at least 6 characters.');return}if(password!==confirmPassword){alert('Passwords do not match.');return}localStorage.setItem('user',JSON.stringify({name,email,mobile,password,address:''}));alert('Account Created Successfully! 🎉');location.href='login.html'}
