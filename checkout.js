let cart=JSON.parse(localStorage.getItem('cart'))||[];const money=v=>parseFloat(String(v).replace('₹','').replace(/,/g,''))||0;let subtotal=cart.reduce((s,x)=>s+money(x.price),0);let delivery=cart.length?40:0;let total=subtotal+delivery;document.getElementById('subtotal').textContent=`Subtotal : ₹${subtotal.toLocaleString('en-IN')}`;document.getElementById('grandtotal').textContent=`Total : ₹${total.toLocaleString('en-IN')}`;
function placeOrder(){if(!cart.length){alert('Your cart is empty 🛒');location.href='index.html';return}const name=document.getElementById('name').value.trim(),mobile=document.getElementById('mobile').value.trim(),address=document.getElementById('address').value.trim(),city=document.getElementById('city').value.trim(),state=document.getElementById('state').value.trim(),pincode=document.getElementById('pincode').value.trim(),payment=document.getElementById('payment').value;if(!name||!mobile||!address||!city||!state||!pincode){alert('Please fill all delivery details.');return}if(!/^\d{10}$/.test(mobile)){alert('Enter a valid 10-digit mobile number.');return}if(!/^\d{6}$/.test(pincode)){alert('Enter a valid 6-digit pincode.');return}let orders=JSON.parse(localStorage.getItem('orders'))||[];orders.unshift({id:'ORD'+Date.now(),date:new Date().toLocaleDateString('en-IN'),status:'Placed',payment,total,items:cart,address:`${address}, ${city}, ${state} - ${pincode}`});localStorage.setItem('orders',JSON.stringify(orders));localStorage.removeItem('cart');alert(`🎉 Order Placed Successfully!\nTotal: ₹${total.toLocaleString('en-IN')}`);location.href='orders.html'}
window.placeOrder=placeOrder;
